// 5. Перепишите myConcat функцию, которая добавляет содержимое в arr2, arr1 чтобы она использовала синтаксис функции стрелки.

const myConcat = (arr1, arr2) => {
    return arr1.concat(arr2);
  };
 
  console.log(myConcat([1, 2], [3, 4, 5]));
